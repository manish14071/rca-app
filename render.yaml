services:
  - type: web
    name: rest-express-backend
    env: node
    plan: free
    buildCommand: npm install && npm run build && npm run build-client
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: chat-app
          property: connectionString